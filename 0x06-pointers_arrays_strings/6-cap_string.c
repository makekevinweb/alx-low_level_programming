#include "main.h"

/**
 *cap_string - capitalizes each word of a string
 * @*: the pointer to character to capitalize
 *
 *Return: a capitalized character
 */
char *cap_string(char *)
{
  while (*)
    {
      if (*[0] <= 'a' && *[0] >= 'z')
	{
	  *[0] = (* - ' ');
	}
      
      if(*s == ',' || *s == ';' || *s == '.' || *s == '!' || *s == '?' || *s == '"' || *s == '(' || *s == ')' || *s == '{' || *s == '}')
	{
	  if (* <= 'a' && * >= 'z')
	    {
	      *++ = (* - ' ');
	    }
	  *++;
	}
    }
  return (*);
}
