#include <stdio.h>
/**
 *main - Entry point
 *
 *Return: Always 0(success)
 */
int main(void)
{
int n;
int m;
n = '0';
m = '0';
while (n <= '9')
{
while (m <= '9')
{
putchar(n);
putchar(m);
putchar(',');
putchar(' ');
m++;
n++;
putchar(n)
}
return (0);
}
