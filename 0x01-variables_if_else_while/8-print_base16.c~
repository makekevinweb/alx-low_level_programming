#include <stdio.h>
/**
 *main - Entry point
 *Return: ALways 0(success)
 */
int main(void)
{
int n;
int m;
for (n = '0'; n <= '9'; n++)
{
putchar(n);
}
for (m = 'a'; n <= 'f'; n++)
{
putchar(m);
}
putchar('\n');
return (0);
}
