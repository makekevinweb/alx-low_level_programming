#include <stdio.h>
/**
 *main - Entry point
 *Return: Always 0(Success)
 */
int main(void)
{
  int c = '0';
  int d = 'a';
  while (c <= '9')
    {
      putchar(c);
      c++;
    }
  while (d <= 'f')
    {
      putchar(d);
      d++;
    }
  putchar('\n');
  return (0);
}
