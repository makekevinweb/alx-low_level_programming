#include <stdio.h>
/**
 *main - Entry point
 *Return: Always 0(Success)
 */
int main(void)
{
int c = '0';
while (c <= '9')
{
putchar(c);
putchar(',');
putchar(' ');
c++;
if (c == '9')
{
putchar(c);
}
}
return (0);
}
