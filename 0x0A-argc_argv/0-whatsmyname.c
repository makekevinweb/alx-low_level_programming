#include <stdio.h>
/**
 *main - prints name of the program
 *@a: number of argument
 *@b: arrays of arguments
 *return : always 0
 */
void main(int a, char *b[])
{
	printf("%s \n", b[0]);
	return (0);
}
