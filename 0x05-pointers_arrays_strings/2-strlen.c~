#include "main.h"
/**
 *_strlen - prints length of a string
 *
 *@s: pointer to string n
 *
 *Return: the length of the string
 */

int _strlen(char *s)
{
int i;
/* the loop runs till the end of the string*/
for (i = 0; s[i] != '\0'; i++)
  {}
return (i);

}
